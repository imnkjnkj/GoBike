import { View, Text } from 'react-native'
import React from 'react'
import TabRoute from '../TabRoute';

const TrainingRoute = () => {
  return (
    <View>
      <TabRoute />
    </View>
  )
}

export default React.memo(TrainingRoute);