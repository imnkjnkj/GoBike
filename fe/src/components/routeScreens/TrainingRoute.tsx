import { View, Text } from 'react-native'
import React from 'react'

const TrainingRoute = () => {
  return (
    <View>
      <Text>TrainingRoute</Text>
    </View>
  )
}

export default React.memo(TrainingRoute);