import { View, Text } from 'react-native'
import React from 'react'

const HealthRoute = () => {
  return (
    <View>
      <Text>HealthRoute</Text>
    </View>
  )
}

export default React.memo(HealthRoute);